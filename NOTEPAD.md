# Notepad

Running scratch notes / reminders for the Inito GEO monitor. Add an entry by typing
`note - ...` (or `note-...`) in chat — it gets appended here as a dated bullet.

## Notes

- 2026-06-20 — Remember to rerun the pipeline for OG (old-device) images — i.e. once image-based
  staleness detection exists, re-run so pages still showing the old iPhone-clip device get flagged.
- 2026-06-20 — Check Ashwin's file + send feedback on the current database.
